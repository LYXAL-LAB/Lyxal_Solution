-- ============================================================================
-- TESTS — fn::utils_string_pad_center
-- Les tests provoquant un THROW doivent être exécutés individuellement.
-- ============================================================================

RETURN fn::utils_string_pad_center(
    "Lyxal",
    11,
    "-"
);
