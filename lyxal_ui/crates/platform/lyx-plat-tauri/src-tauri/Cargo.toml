[package]
name = "lyx-platform-src-tauri"
version = "0.8.0"
edition = "2024"

[lib]
name = "lyx-platform-src-tauri"
path = "src/lib.rs"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "*", features = [] }

[dependencies]
tauri = { version = "*", features = ["devtools"] }
serde_json = { version = "*" }
#tauri-plugin-devtools = { version = "*" }

[features]
#default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]




