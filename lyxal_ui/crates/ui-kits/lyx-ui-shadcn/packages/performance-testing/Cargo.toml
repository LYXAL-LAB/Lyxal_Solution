[package]
name = "lyx-ui-performance-testing"
version = "0.8.0"
edition = "2024"
[dependencies]
# Core dependencies
tokio = { version = "*", features = ["full"] }
serde = { version = "*", features = ["derive"] }
serde_json = { version = "*" }
anyhow = { version = "*" }
thiserror = { version = "*" }
# Time handling
chrono = { version = "*", features = ["serde"] }
web-time = { version = "*" }
# File system operations
walkdir = { version = "*" }
# System information
sysinfo = { version = "*" }
# Logging
log = { version = "*" }
env_logger = { version = "*" }
[dev-dependencies]
tempfile = { version = "*" }
tokio-test = { version = "*" }
pretty_assertions = { version = "*" }
[features]
default = ["benchmarks", "regression", "reporting"]
benchmarks = []
regression = []
reporting = []
ci-integration = []
[[bin]]
name = "lyx-ui-performance-testing"
path = "src/lib.rs"
# [[bench]]
# name = "component_benchmarks"
# harness = false




