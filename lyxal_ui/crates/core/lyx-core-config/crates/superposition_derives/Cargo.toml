[package]
name = "lyx-core-superposition_derives"
version = "0.0.1"
edition = "2024"

[dependencies]
syn = { version = "0.0.1", features = ["full"] }
quote = { version = "0.0.1" }
proc-macro2 = { version = "0.0.1" }

[lib]
proc-macro = true

[features]
diesel_derives = []
