[package]
name = "lyx-core-tachys"
version = "0.0.1"
edition = "2024"

[dependencies]
lyx-core-reactive_graph = { path = "../reactive_graph", version = "0.0.1" }
wasm-bindgen = { version = "0.0.1" }
web-sys = { version = "0.0.1", features = ["Element", "Node"] }

[features]
islands = []
nonce = []
nightly = []
csr = []
ssr = []
hydrate = []
