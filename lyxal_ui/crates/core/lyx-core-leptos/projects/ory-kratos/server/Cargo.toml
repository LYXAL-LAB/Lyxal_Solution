[package]
name = "server"
version = "*"
edition = "2024"
[dependencies]
app = { version = "*", path = "../../../../../platform/lyx-plat-tauri/app", optional = false, default-features = false, features = ["ssr"] }
leptos = { version = "*", path = "../../../leptos", optional = false, features = [ "ssr" ] }


[build-dependencies]
rustc_version = "*"


