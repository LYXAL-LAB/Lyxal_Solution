[package]
name = "src"
version = "0.8.0"
edition = "2024"

[lib]
path = "lib.rs"

[dependencies]
leptos = { path = "../../leptos/leptos", optional = true }
tokio = { version = "*", features = ["full"], optional = true }

[features]
default = []
ssr = ["leptos/ssr", "dep:tokio"]










