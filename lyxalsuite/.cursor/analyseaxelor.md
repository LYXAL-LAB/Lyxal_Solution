Tu dois analyser en profondeur le module `axelor-<module>` comme cela a été fait pour `axelor-base`.

Tu dois produire un fichier `analyse.md` dans le dossier :
`lyxalsuite/lyxal<nom_module>/analyse.md`

Exemple :
→ `lyxalsuite/lyxalcrm/analyse.md`

‼️ Tu dois **strictement respecter la structure et la profondeur** du module déjà analysé `lyxalbase` (fichiers : 00-sommaire.md à 07-points-critiques.md).

‼️ Ton rapport doit contenir **toutes les sections suivantes**, complètes et correctement documentées :

1. Nom du module  
2. Objectif métier  
3. Fonctions principales  
4. Tables utilisées  
5. Routes API REST exposées  
6. Logique métier  
7. Services métier  
8. Interfaces techniques  
9. États et workflows  
10. Dépendances croisées  
11. Points critiques  
12. Fichiers analysés (Java, XML, etc.)

❌ Tu ne dois pas générer de code.  
❌ Tu ne dois pas adapter à TypeScript.  
❌ Tu dois uniquement **analyser et documenter le module original Axelor**.

Ton seul livrable est un fichier Markdown nommé `analyse.md` dans le bon dossier.

Base-toi **strictement** sur l’exemple de `lyxalbase/analyse.md`, qui est désormais **la référence unique**.

