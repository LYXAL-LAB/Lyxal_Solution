# Why you should use Sōzu

- **Hot configurable:** Lyxal_proxy can receive configuration changes at runtime through secure unix sockets.
- **Upgrades without restarting:** Lyxal_proxy is always-up, meaning it upgrades itself *while still processing requests*.
