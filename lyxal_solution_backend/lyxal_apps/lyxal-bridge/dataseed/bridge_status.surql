---- Valeurs des seeds ----

    CREATE bridge_status:active CONTENT {
        name: 'active',
        description: 'Active',
        is_enabled: true,
    };

    CREATE bridge_status:inactive CONTENT {
        name: 'inactive',
        description: 'Inactive',
        is_enabled: true,
    };

    CREATE bridge_status:suspended CONTENT {
        name: 'suspended',
        description: 'Suspended',
        is_enabled: true,
    };

    CREATE bridge_status:banned CONTENT {
        name: 'banned',
        description: 'Banned',
        is_enabled: true,
    };

    CREATE bridge_status:locked CONTENT {
        name: 'locked',
        description: 'Locked',
        is_enabled: true,
    };  