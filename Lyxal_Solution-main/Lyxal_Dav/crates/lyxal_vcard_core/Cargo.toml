[package]
name = "lyxal_vcard_core"
version = "0.1.0"
edition = "2021"
description = "Lyxal vCard core (Rust-only, source of truth for CardDAV)"
license = "MIT"

[lib]
name = "lyxal_vcard_core"
path = "src/lib.rs"

[dependencies]
thiserror = "2.0"
chrono = { workspace = true }

