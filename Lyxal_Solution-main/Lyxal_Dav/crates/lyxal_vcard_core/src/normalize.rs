use crate::types::VCard;

pub fn normalize(_vcard: &mut VCard) {
    // Apple-compatible normalization placeholder
    // Could ensure X-APPLE properties are preserved or ordered
    // For now, no-op or basic cleanup
}

