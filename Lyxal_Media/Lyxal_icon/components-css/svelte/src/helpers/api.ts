import { setFetch, fetchJSON } from '@lyxal-icon/fetch';
import { createLyxalAPILoader } from '@lyxal-icon/component-utils/loader/api/create';
import {
	setLoader,
	setProviderLoader,
	getLoader,
} from '@lyxal-icon/component-utils/loader/loaders';
import type { LoaderConfig } from '@lyxal-icon/component-utils/loader/types';

export {
	setFetch,
	fetchJSON,
	createLyxalAPILoader,
	setLoader,
	setProviderLoader,
	getLoader,
};

export type { LoaderConfig };
