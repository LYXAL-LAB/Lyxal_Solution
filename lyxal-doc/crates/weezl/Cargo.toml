[package]
name = "weezl"
version = "0.1.12"
edition = "2018"
description = "Fast LZW compression and decompression (vendored)"
license = "MIT OR Apache-2.0"

[dependencies]
# No dependencies - pure Rust implementation

[features]
default = ["std"]
std = ["alloc"]
alloc = []
