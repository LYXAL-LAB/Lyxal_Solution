[package]
name = "equivalent"
version = "1.0.2"
edition = "2021"
description = "Traits for key comparison in maps (vendored)"
license = "Apache-2.0 OR MIT"

[dependencies]
# No dependencies - pure Rust trait
