[package]
name = "fax_derive"
version = "0.2.0"
edition = "2021"
description = "Proc-macro for CCITT fax bitmaps (vendored)"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "parsing"] }
