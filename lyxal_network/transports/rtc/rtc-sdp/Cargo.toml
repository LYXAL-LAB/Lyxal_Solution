[package]
name = "rtc-sdp"
version.workspace = true
edition.workspace = true

[dependencies]
shared = { workspace = true, default-features = false, features = [] }

url = "2.5.0"
rand = "0.8.5"

[dev-dependencies]
criterion = "0.5.1"
